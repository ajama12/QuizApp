$(document).ready(() => {

//create quiz template
const createQuiz = (quiz) => {

return `
  <div class="quiz-row">
      <button class="quiz-item">${quiz.quiz_name}</button>
  </div>
  `
}



















});
